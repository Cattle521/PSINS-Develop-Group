CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(PSINS)

if (CMAKE_COMPILER_IS_GNUCXX)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DPSINS_RMEMORY")
endif()

INCLUDE_DIRECTORIES(./)
ADD_LIBRARY(PSINS PSINS.cpp)

ADD_SUBDIRECTORY(demo1)
